# UAV Visual RL — Python dependencies
# Install: pip install -r requirements.txt
# The GPU training/render path additionally needs a CUDA-capable torch
# build and gsplat compiled for that CUDA version (see README / deploy docs).

numpy>=1.24
scipy>=1.10
gymnasium>=0.29
torch>=2.1
torchvision>=0.16
gsplat>=1.5
plyfile>=1.0
Pillow>=10.0
matplotlib>=3.7
pymavlink>=2.4
