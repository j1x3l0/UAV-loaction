# 理解门槛清单 — UAV Visual RL 3D Path Planning

> 作用：合并前最后一步的"我懂了吗"清单。由 `@understanding-gate` Agent 走查，可阻止合并。
> 4 层理解模型：定位 / 数据流 / 边界 / 故障。"理解而非正确性"的判断核心抽象。

---

## PR 基础信息

- **PR 标题**：<标题>
- **关联 Spec**：<docs/specs/xxx.md 或"无">
- **改动概览**：<一句话>
- **审查者**：<人 / Agent>
- **日期**：<YYYY-MM-DD>

---

## 4 层理解检查

### 第 1 层 — 定位（它在哪里）

- [ ] 我能在 30 秒内说出本改动在架构中的位置（envs/core/scripts 三层中的哪一层？）
- [ ] 我能指出它直接依赖的上游模块和下游模块
- [ ] 我能在 [docs/knowledge-map.md](docs/knowledge-map.md) 中找到它的对应行（或确认已新增）

**判定**: PASS / FAIL
**证据**: <文件路径:行号 / 一句话>

### 第 2 层 — 数据流（数据怎么流）

- [ ] 我能在不看代码的情况下用一句话说清 输入 → 变换 → 输出
- [ ] 如果涉及深度图: 64×64×1 → CNN → 128D → +6D vec → 134D → MLP → action(3) + value(1)
- [ ] 异常路径也有数据流说明（碰撞/超时/边界hit → 如何影响下一步观测？）

**判定**: PASS / FAIL
**数据流一句话**: <输入 X → 变换 Y → 输出 Z>

### 第 3 层 — 边界（它做什么 / 不做什么）

- [ ] 我能说出它"明确不负责"的事（不只是"负责"）
- [ ] 它没有越界处理本该属于其他模块的职责
- [ ] 环境不负责训练；Agent不负责环境交互；degradation_utils不负责物理

**判定**: PASS / FAIL
**不负责清单**: <3-5 条>

### 第 4 层 — 故障（它会怎么坏）

- [ ] 我能说出最可能的 1-3 个故障模式和触发条件
- [ ] 深度图全零 / 全max_depth → Agent行为？
- [ ] 退化配置缺失 → fallback行为明确？
- [ ] NaN在深度图/动作/value中 → 会传播到哪里？

**判定**: PASS / FAIL
**最可能的故障模式**: <1-3 条>

---

## 综合判定

- [ ] 第 1 层 PASS — 定位清晰
- [ ] 第 2 层 PASS — 数据流可追踪
- [ ] 第 3 层 PASS — 边界明确
- [ ] 第 4 层 PASS — 故障可预测

### 结论: [通过 / 条件通过 / 阻止合并]

- **通过**: 4 层全 PASS → 可合并
- **条件通过**: 标注 ≤2 项待澄清，每项有明确补救计划 → 可合并并跟进
- **阻止合并**: 任一层 FAIL 且无补救 → 必须回到写代码/Microworld 阶段

### 条件通过清单（如适用）

| # | 待澄清项 | 补救计划 | 负责人 | due date |
|---|----------|----------|--------|----------|
| 1 | <…> | <…> | <…> | <…> |

---

> Code Review 的终点不是 LGTM，而是"我懂了"。任何"看起来对但说不清"的代码都应被拦截。
