# UAV Agent RL v1 - 项目整理文档

> 基于深度强化学习的无人机三维路径规划系统  
> 整理时间：2026-07-08

---

## 一、项目概述

本项目是一个基于 **PPO** 和 **SAC** 算法的无人机三维路径规划系统，支持深度传感器输入与向量状态混合观测空间。项目已完成模块化重构，当前处于实验性开发阶段，PPO 算法可用，SAC 算法待进一步优化。

### 核心特性
- **混合观测空间**：深度图像（16x16）+ 向量状态（位置、速度、目标方向）
- **双算法支持**：PPO（推荐）、SAC（实验性）
- **模块化训练**：支持独立运行、对比实验和基线评估
- **完整评估体系**：成功率、碰撞率、超时率、奖励统计

---

## 二、系统架构

```
┌─────────────────────────────────────────┐
│           无人机路径规划系统               │
├─────────────────────────────────────────┤
│  观测层   │  深度图像(16x16) + 向量状态   │
├─────────────────────────────────────────┤
│  编码层   │  CNN图像编码器 + MLP状态编码器 │
├─────────────────────────────────────────┤
│  算法层   │  PPO Agent / SAC Agent        │
├─────────────────────────────────────────┤
│  环境层   │  DroneEnv (Gymnasium)         │
├─────────────────────────────────────────┤
│  评估层   │  成功率/碰撞率/超时率/奖励曲线  │
└─────────────────────────────────────────┘
```

---

## 三、核心文件清单

### 3.1 环境与智能体

| 文件 | 功能 | 状态 |
|------|------|------|
| `drone_env.py` | 无人机三维路径规划环境，支持深度传感器和向量状态混合观测 | 核心 |
| `drone_space_config.py` | 动作/观测空间配置 | 核心 |
| `ppo_agent.py` | PPO智能体，支持深度图像编码器和向量状态编码器 | 推荐 |
| `sac_agent.py` | SAC智能体（实验性） | 待优化 |
| `model.py` | 神经网络模型定义 | 核心 |

### 3.2 训练与评估

| 文件 | 功能 | 状态 |
|------|------|------|
| `train.py` | 主训练脚本，支持PPO/SAC/对比实验 | 推荐 |
| `training.py` | 训练逻辑封装 | 核心 |
| `modular_trainer.py` | 模块化训练器 | 核心 |
| `evaluate_ppo_model.py` | PPO模型评估（成功率、碰撞率等） | 推荐 |
| `baseline_evaluation.py` | 基线算法对比评估 | 可用 |
| `config.py` | 统一训练参数配置 | 核心 |

### 3.3 五步基线训练流程（历史版本）

| 步骤 | 文件 | 说明 |
|------|------|------|
| 1 | `train_ppo_v1.py` | PPO-V1 基线训练 |
| 2 | `train_ppo_v2.py` | PPO-V2 奖励函数改进 |
| 3 | `train_sac_v1.py` | SAC-V1 基线训练 |
| 4 | `train_sac_v2.py` | SAC-V2 奖励函数改进 |
| 5 | `baseline_comparison.py` | 四组结果对比分析 |

> 五步流程用于基线评估和算法对比，与 `train.py` 的主流程并行存在。

### 3.4 测试与工具

| 文件 | 功能 |
|------|------|
| `simple_test.py` | 环境简单测试 |
| `test_model.py` | 模型加载与推理测试 |
| `test_smoke.py` | 冒烟测试 |

### 3.5 文档与报告

| 文件 | 内容 |
|------|------|
| `README.md` | 项目完整说明 |
| `TRAINING_GUIDE.md` | 五步训练流程使用指南 |
| `future_improvements.md` | 未来改进方向（9大模块） |
| `cnn_optimization_report.md` | CNN轻量级架构优化报告 |
| `depth_image_size_comparison_report.md` | 深度图像16x16 vs 64x64对比 |
| `rl_lib_vs_isaac_gym.md` | RLlib与Isaac Gym技术选型对比 |

---

## 四、快速开始

### 4.1 环境安装

```bash
pip install -r requirements.txt
# 主要依赖：torch, numpy, gymnasium, tensorboard, matplotlib
```

### 4.2 训练模型

```bash
# PPO 完整训练（推荐）
python train.py --algorithm ppo --episodes 5000 --depth-size 16 --batch-size 10

# SAC 实验性训练
python train.py --algorithm sac --episodes 3000 --depth-size 16 --batch-size 10

# PPO vs SAC 对比实验
python train.py --algorithm compare
```

### 4.3 评估模型

```bash
python evaluate_ppo_model.py
```

### 4.4 查看训练日志

```bash
tensorboard --logdir logs/
# 访问 http://localhost:6006
```

---

## 五、关键实验结果

### 5.1 最新模型性能（PPO，5000轮，2026-01-29）

| 指标 | 数值 | 说明 |
|------|------|------|
| 成功率 | 0.00% | 需进一步训练优化 |
| 碰撞率 | 36.00% | 障碍物规避能力有限 |
| 超时率 | 64.00% | 大部分episode未能到达目标 |
| 平均最终距离目标 | 8.73米 | 距离目标仍较远 |

> **结论**：当前模型收敛不足，建议继续训练或优化奖励函数。

### 5.2 CNN架构优化成果

| 指标 | 优化前（4层） | 优化后（2层轻量） | 变化 |
|------|--------------|------------------|------|
| 参数量 | ~4.2M | ~12K | **减少99.7%** |
| SAC最佳奖励 | 0.3018 | 0.5428 | **+79.8%** |
| PPO最佳奖励 | 0.5369 | 0.5054 | -5.9%（可接受） |
| 训练速度 | 10-11步/秒 | 10步/秒 | 保持稳定 |

> **结论**：16x16深度图像 + 2层轻量CNN是推荐配置，特别适合边缘设备部署。

### 5.3 深度图像尺寸对比（16x16 vs 64x64）

- **16x16**：计算量减少16倍，PPO测试表现更优（0.5369 vs 0.3621），适合实时应用
- **64x64**：信息更丰富，但计算开销大，适合需要精细感知的场景

---

## 六、模型与实验产物

### 6.1 保存的模型

| 模型 | 位置 | 说明 |
|------|------|------|
| `drone_ppo_model.pth` | 根目录 | 主模型 |
| `drone_ppo_model_v2.pth` | 根目录 | V2版本 |
| `drone_ppo_model_v2_curriculum.pth` | 根目录 | 课程学习版本 |
| `saved_models/` | 子目录 | 按时间戳保存的训练模型 |
| `models/baseline/` | 子目录 | 基线评估最佳模型 |
| `models/ppo_drone_v3_*.pth` | 子目录 | V3版本模型 |

### 6.2 实验结果目录

- `results/`：18组实验结果（含config、experiment_report、training_summary）
- `logs/`：28+组TensorBoard训练日志
- `comparison_results/`：PPO vs SAC对比结果

---

## 七、待办改进清单

基于 `future_improvements.md`，按优先级排序：

### 高优先级
1. **奖励函数优化**
   - 引入指数衰减距离奖励
   - 添加速度惩罚与动作平滑奖励
   - 尝试分层奖励函数
2. **增加训练轮数**
   - 当前5000轮尚未收敛，建议继续训练至10000+轮
3. **动态障碍物**
   - 在环境中添加移动障碍物，提升场景复杂度

### 中优先级
4. **环境复杂度提升**
   - 添加风力干扰（随机外力）
   - 添加传感器噪声
5. **注意力机制/CNN优化**
   - 在轻量CNN中集成SE或CBAM模块
   - 多尺度特征融合
6. **评估指标扩展**
   - 增加"平均与障碍物距离"、"最大速度"、"成功到达时间"等指标

### 低优先级
7. **与其他框架整合**
   - Isaac Gym：高精度物理模拟
   - ROS：真实无人机通信
8. **实时可视化**
   - Matplotlib动画展示飞行轨迹
9. **模型轻量化/知识蒸馏**
   - 进一步压缩模型体积

---

## 八、技术选型说明

| 方案 | 当前选择 | 备选方案 | 建议 |
|------|---------|---------|------|
| RL框架 | 自研PPO/SAC | RLlib | 自研更轻量，RLlib适合分布式 |
| 物理仿真 | 简化物理模型 | Isaac Gym | 当前够用，高精度需求时迁移 |
| 图像尺寸 | 16x16 | 32x32 / 64x64 | 16x16已验证为最佳平衡点 |
| CNN架构 | 2层轻量CNN | 4层深度CNN | 轻量架构参数量减少99.7% |

---

## 九、项目状态总结

| 模块 | 完成度 | 状态 |
|------|--------|------|
| 环境搭建 | 100% | 稳定可用 |
| PPO算法 | 90% | 可用，需更多训练 |
| SAC算法 | 60% | 实验性，不稳定 |
| CNN优化 | 100% | 已完成轻量级重构 |
| 评估系统 | 90% | 可用，指标可扩展 |
| 基线对比 | 100% | 五步流程已完成 |
| 文档报告 | 100% | 6份报告已整理 |
| 模型收敛 | 30% | 成功率仍为0%，需重点优化 |

**下一步重点**：奖励函数调优 + 延长训练轮数 + 动态障碍物。

---

*本文档由项目文件自动整理生成，可直接复制粘贴至飞书文档。*
