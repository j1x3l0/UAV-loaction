# RULES.md — 工程规则锚定模板（认知债务防御 V2 第 1 防御层）

> 作用："约束在前，代码在后。规则后置的成本是前置的 3×。"
> 本文件锁定项目的技术栈、目录结构、命名规范、安全红线、质量门槛。
> 在为本项目生成任何代码之前，请先理解并锁定以下规则，之后的每次生成都必须遵守。
> 删除所有 `<占位符>` 后投入使用。对应提示词手册 V2「场景 0：工程规则锚定」。

---

## 1. 技术栈

- **框架**: <例如 Next.js 14 / Vue 3 / FastAPI / ...>
- **语言**: <例如 TypeScript strict / Python 3.12+>
- **运行时**: <例如 Node.js 20 / Python 3.12>
- **包管理**: <例如 pnpm / poetry>
- **数据库**: <例如 PostgreSQL 16 + Prisma / SQLAlchemy>
- **禁止依赖**: <列出明确不能用的库，并简短说明原因>

## 2. 目录结构

```
<项目根>/
├── src/api/        → 路由层（只做协议转换，不放业务）
├── src/services/   → 业务逻辑
├── src/models/     → 数据层
├── docs/
├── microworld/
└── ...
```

## 3. 代码规范

- **命名**: <camelCase 变量 / PascalCase 组件 / UPPER_SNAKE 常量 / ...>
- **注释**: <所有 public 函数必须有 JSDoc/docstring，无解释意图的代码不允许>
- **错误处理**: <统一返回 { data, error } 结构 / Result 类型 / ...>
- **日志**: <禁止裸 print，统一走 logger，且不要打敏感信息>

## 4. 安全红线（绝对禁止）

- 禁止硬编码密钥、Token、密码（必须走环境变量或密钥管理）
- 禁止使用 `eval()`、`exec()` 或等效动态执行
- 禁止忽略输入校验（外部输入必须经 schema 校验）
- 禁止把内部异常堆栈直接返回给用户
- <补充项目特有红线>

## 5. 质量门槛 —— 代码交付前必须通过

- [ ] Lint 0 warning
- [ ] 类型检查通过（`tsc --noEmit` / `mypy` / ...）
- [ ] 3 种输入测试通过：正常输入 / 空输入 / 异常输入
- [ ] 无硬编码密钥
- [ ] 关键函数的错误处理完整
- [ ] 不引入未被本规则允许的依赖

## 6. 边界与解释权

- 本规则一经锁定，AI 在本次会话及后续会话中不得擅自变更。
- 如需新增/修改规则，必须由人类明确确认；AI 只能建议，不能自决。
- 与本规则冲突的代码视为"未通过质量门槛"，禁止交付。